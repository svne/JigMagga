steal('test', function () {
    "use strict";

    module("<%= Namespace %>.Jig.<%= FullName.join('.') %>", {
        setup: function () {
            F.open("/<%= namespace %>/jig/<%= fullName.join('/') %>/test/<%= name %>.html", function(){
                stop()
                F.win.steal.config("domain", "default");
                F.win.steal("<%= namespace %>/jig/<%= fullName.join('/') %>/test/<%= name %>.conf", function(){
                start();
                });
            });
        }
    });

    test("visible Test", function () {
        //TODO implement testacse for <%= Namespace %>.Jig.<%= FullName.join('.') %>
        ok(false, "no testcase")
    });
});
