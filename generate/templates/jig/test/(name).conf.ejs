{
    "jigs" : {
        ".<%= namespace %>-jig-<%= fullName.join('-') %>" : {
            "controller" : "<%= Namespace %>.Jig.<%= FullName.join('.') %>",
            "template": "<%= namespace %>/jig/<%= fullName.join('/') %>/views/init.mustache",
            "css" : "<%= namespace %>/jig/<%= fullName.join('/') %>/css/<%= name %>.scss",
            "render": true,
            "prerender": true
        }
    }
}
