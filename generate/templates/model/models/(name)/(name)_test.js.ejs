steal('unit')
    .then("<%= namespace %>/fixture/fixtures.js")
    .then( "<%= namespace %>/models/<%= name %>", function() {
        "use strict";
        
        module("Yd.Models.<%= Name %>", {
            setup: function () {
            }
        });

        test("<%= Name %> validity test", function () {
            // TODO: implement testacse for <%= Namespace %>.Model.<%= Name %>
            ok(false, "no testcase")
        });
});
