{
    "jigs" : {
        ".jig-<%= name %>" : {
            "controller" : "Jig.<%= name %>",
            "template": "jig/<%= name %>/views/<%= name %>.mustache",
            "css" : "jig/<%= name %>/css/<%= name %>.scss"
        }
    }
}