<!doctype html><!--[if lte IE 7 ]>
<html lang="de" id="de" class="ie7"><![endif]--> <!--[if IE 8 ]>
<html lang="de" id="de" class="ie8"><![endif]--> <!--[if IE 9]>
<html lang="de" id="de" class="ie9"><![endif]--> <!--[if gt IE 9]><!-->
<html lang="de" id="de" class="modern"><!--<![endif]-->
<head>
    <meta charset="utf-8" />
</head>
<body data-pagename="<%= name %>">

    <div class="<%= namespace %>-header">
        <div class="<%= namespace %>-header-inner">
            <section class="<%= namespace %>-jig-header"></section>
        </div>
    </div>

    <div class="<%= namespace %>-wrapper">

        <div class="<%= namespace %>-content">
            <div class="<%= namespace %>-content-inner">
                <!-- put your content jigs here (with or without slot system) -->
            </div>
        </div>

        <div class="<%= namespace %>-footer">
            <div class="<%= namespace %>-footer-inner">
                <section class="<%= namespace %>-jig-footer"></section>
            </div>
        </div>

    </div>

    <script id="<%= namespace %>-application-script" type='text/javascript' src='/steal/steal.js?<%= namespace %>/page/<%= domain%>/<%= name %>'></script>
</body>
</html>