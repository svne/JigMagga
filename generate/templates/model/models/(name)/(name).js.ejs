steal('core/model', function(){

/**
 * @class <%= name %>
 * @parent core/model
 * @inherits can.Model
 * Wraps <%= namespace %> backend services.
 */
can.Model.extend("<%= Namespace %>.Models.<%= Name %>",
    /* @Static */
    {
        findAll: "GET /api/<%= name %>s",
        findOne : "GET /api/<%= name %>s/{id}",
        create : "POST /api/<%= name %>s",
        update : "PUT /api/<%= name %>s/{id}",
        destroy : "DELETE /api/<%= name %>s/{id}"
    },
    /* @Prototype */
    {});
});
