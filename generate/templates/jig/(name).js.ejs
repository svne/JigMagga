steal('core/control',
    '<%= namespace %>/media/css/<%= namespace %>-core.scss'
).then(
    './css/<%= name %>.scss',
    function () {
    "use strict";

    /**
     * @class <%= name %>
     */
    can.Control.extend('<%= Namespace %>.Jig.<%= FullName.join('.') %>',
    /** @Static */
        {
            defaults : {
                template: "//<%= namespace %>/jig/<%= fullName.join('/') %>/views/init.mustache",
                message: "test message"
            }
        },
        /** @Prototype */
        {
            init : function () {
                var self = this;
                self.element.html(can.view(self.options.template, self.options));
            }
        });
});
