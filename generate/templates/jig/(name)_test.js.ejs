steal('test', function () {
    "use strict";

    module("<%= Namespace %>.Jig.<%= Name %>", {
        setup: function () {
            F.open(steal.URI( "./" ).normalize().toString() + "<%= name %>.html");
        }
    });

    test("visible Test", function () {
        //TODO implement testacse for <%= Namespace %>.Jig.<%= Name %>
        ok(false, "no testcase")
    });
});